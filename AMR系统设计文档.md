# FXXXXXN AMR CONTROL 产品与系统设计总览

## 1. 文档定位

本文档用于帮助产品、前端、后端和现场实施人员快速理解平台目标、业务闭环与模块关系。详细模块边界以《系统架构》为准，页面字段与验收以《前端UI需求文档》为准。

当前原型只涉及前端页面和演示交互，不定义通信协议、路径规划算法、调度算法或后端部署架构。

## 2. 产品范围

- 场景：厂内CNC流水线物流。
- 首期：单一AMR品牌、平台直接调度单台车辆、简单地图与任务。
- 核心目标：形成设备请求、任务生成、AMR调度、行为树执行、交通监控和事件关闭的完整链路。
- 扩展方向：多品牌、多楼层、多地图、多车型和复杂交通资源。

## 3. 黄金业务链

业务场景：CNC-01加工完成，请求AMR-01将成品搬运至BUF-01。

```mermaid
flowchart LR
  A["CNC-01 发出加工完成请求"] --> B["派单管理生成任务"]
  B --> C["调度策略选择 AMR-01"]
  C --> D["行为树执行取货与运输"]
  D --> E["交通监控申请并释放道路资源"]
  E --> F["BUF-01 确认收货"]
  F --> G["任务关闭并形成任务日志"]
```

链路需要可以从CNC设备详情、任务详情、AMR详情、行为树实例、交通记录和任务日志相互追踪。

## 4. 信息架构

```text
首页
派单管理
├─ 任务中心
├─ 任务配置
├─ 调度策略
└─ 任务日志
行为树管理
├─ 行为树监控
└─ 行为树列表
   └─ 行为树编辑器
交通监控
├─ 实时交通
└─ 交通记录
   └─ 交通记录详情
地图管理
├─ 地图管理
├─ 地图日志
└─ 地图编辑器
资源管理
├─ AMR列表
│  └─ AMR详情
├─ 设备列表
│  └─ 设备详情
├─ AMR型号
└─ 设备类型
调试平台
├─ AMR调试
├─ API调试
└─ 调试记录
平台设置
├─ 用户管理
├─ 角色权限
├─ 配置管理
├─ 数据字典
├─ 操作日志
└─ 系统日志
```

## 5. 全局运行范围

顶部栏统一选择楼层和地图。首页、派单管理、行为树、交通监控、地图管理、资源管理和调试平台共享该范围，业务页面不得重复设置楼层或地图筛选器。

切换范围前需要提示对当前编辑、调试或运行操作的影响。详情页应保留对象原始地图上下文，不能因全局范围变化展示错误对象。

## 6. 地图模型

地图由两个层次组成：

- 扫描底图：AMR雷达扫描或文件导入得到的灰度点云/占用栅格，只用于地图编辑和校准。
- 逻辑地图：道路、节点、设备点位、等待点、禁行区、限速区和交通资源，由地图编辑器维护并随地图版本发布。

地图编辑器支持底图、逻辑图和叠加三种视图。交通监控和AMR调试只读取发布后的逻辑地图，不展示点云。

## 7. 交通监控

实时交通以逻辑地图为主体，展示任务最优路径、AMR当前路径、实际轨迹和重规划点。左侧展示当前地图AMR，右侧展示路径指标与最近事件，地图底部展示实际坐标和目标坐标。

交通资源不作为独立业务页面。窄通道、交叉路口、互斥区和限速区在地图编辑器中定义，随地图版本校验、发布和下发。

交通记录按单次路径执行聚合，详情支持路径回放、规划与实际距离对比、重规划和资源事件追踪。

## 8. AMR资源模型

AMR型号负责定义可复用的运动学能力：

- 底盘类型：差速驱动、全向驱动、舵轮。
- 最大线速度。
- 最大角速度。
- 加速度。
- 到达阈值。
- 前视距离。

AMR实例负责身份和部署信息：

- AMR编号与名称。
- 所属型号。
- 当前地图与初始点位。
- 启用状态。
- 运行状态、电量、位置、速度、连接和当前任务等实时信息。

实例继承型号参数，MVP阶段不允许单车覆盖。调试页面可以设置本次会话的临时限速，但不得超过型号最大线速度。

## 9. AMR调试

AMR调试复用交通监控的AMR卡片和逻辑地图组件。工程师先选择当前地图AMR，再选择以下一种调试方式：

- 任务：选择任务模板、起点设备和目标设备。
- 行为树：选择行为树版本、入口节点和失败处理。
- 指定坐标：选择参考点位或输入X、Y和朝向。

调试继续使用路径规划、行为树和交通资源规则，不得绕过生产安全边界。所有操作进入统一调试记录。

## 10. 状态与日志

- 任务状态：待处理、待调度、执行中、等待中、异常、完成。
- AMR状态：空闲、执行中、等待、充电中、故障、离线。
- 设备状态：空闲、加工中、请求搬运、等待AMR、异常、离线。
- 交通状态：空闲、已占用、等待授权、临时封锁、异常。

任务日志记录黄金链路中的业务事件；交通记录记录路径与资源事件；调试记录记录研发调试；操作日志记录用户配置操作；系统日志用于技术排障。

## 11. 前端实现约定

- 采用静态HTML、共享CSS和Vanilla JavaScript，不安装大型前端依赖。
- 每个一级、二级和三级页面都有独立HTML入口。
- 页面标题、按钮、表格操作列、状态颜色和空状态保持全局一致。
- 详情页通过列表“查看”进入，并提供明确返回按钮。
- 高风险动作必须二次确认，调试、暂停、封锁和地图发布必须留下记录。

## 12. MVP不包含

- 跨品牌协议适配实现。
- 调度、避障、SLAM和路径规划算法。
- 真实后端接口与持久化。
- 多租户、计费和移动端应用。
- 自动化测试平台和生产级权限服务。
