# FXXXXXN AMR CONTROL 系统架构

> 文档状态：当前开发基线
>
> 适用阶段：MVP 前端 UI 原型
>
> 基线版本：V0.2 / 2026-08-05
>
> 当前目标：围绕“CNC 加工完成，请求 AMR 将成品搬运至缓冲区”形成完整业务闭环。

## 1. 系统定位

FXXXXXN AMR CONTROL 是面向厂内物流场景的 AMR 管理与调度平台。当前原型只考虑单一 AMR 品牌、平台直接调度单台车辆和厂内流水线作业，但页面架构保留后续多品牌、多车辆和复杂场景的扩展能力。

前端原型主要负责：

- 展示 CNC、AMR、站点、任务和交通资源的实时状态。
- 展示设备请求生成任务、调度 AMR、行为树执行和交通资源申请的完整过程。
- 提供地图、任务、调度策略、行为树和资源的配置入口。
- 为研发提供 AMR 调试、API 调试、日志和平台配置能力。
- 对成功和失败场景形成可追踪的业务闭环。

协议适配、调度算法、导航算法、交通算法和设备通信实现不属于当前前端原型范围。

## 2. 黄金业务链

```mermaid
flowchart LR
    A[CNC-01 加工完成] --> B[生成设备请求]
    B --> C[匹配任务配置]
    C --> D[生成搬运任务]
    D --> E[调度策略选择 AMR-01]
    E --> F[启动行为树执行实例]
    F --> G[持续申请交通资源]
    G --> H[AMR-01 到达 CNC-01]
    H --> I[完成取货]
    I --> J[运输至 BUF-01]
    J --> K[缓冲区确认接收]
    K --> L[任务完成或失败关闭]
```

建议演示数据统一使用：

| 对象 | 示例编号 |
|---|---|
| CNC 设备 | CNC-01 |
| 设备请求 | REQ-260804-001 |
| 任务配置 | CFG-002 |
| 任务实例 | TSK-260804-001 |
| 任务日志 | LOG-001（可关联 DSP-260804-001） |
| 执行车辆 | AMR-01 |
| 行为树实例 | BTI-260804-001 |
| 目标缓冲区 | BUF-01 |
| 全局追踪号 | TRACE-260804-001 |

所有相关详情页应通过关联对象导航和 Trace ID 互相跳转。

## 3. 一级信息架构

```text
首页
派单管理
行为树管理
交通监控
地图管理
资源管理
调试平台
平台管理
```

平台顶部提供全局运行范围：

```text
专案 / 楼层 / 地图
```

全局运行范围用于筛选实时数据。历史记录和详情页始终显示对象实际所属范围，不因全局筛选变化而改变。

### 3.1 视觉系统原则

平台统一采用浅色简洁 SaaS 风格：

- 首页数字孪生、实时监控、列表、详情和配置页面统一使用白色与冷灰色表面。
- 顶部导航保留深蓝黑作为稳定的品牌锚点，其余工作区域避免大面积深色背景。
- 蓝色作为主操作和导航色，青色用于实时数据、通信和数字孪生路径。
- 监控卡片通过低对比边框、轻阴影和状态色表达层级，不依赖霓虹或高饱和背景。
- 所有页面统一状态色、数据字体、圆角、边框、间距和交互反馈。

统一状态色：

| 状态 | 颜色 |
|---|---|
| 正常、完成、可用 | 绿色 |
| 执行中、已分配 | 蓝色 |
| 实时数据、通信 | 青色 |
| 等待、排队、提醒 | 黄色 |
| 异常、失败、严重告警 | 红色 |
| 未开始、停用、历史 | 灰色 |

## 4. 首页

```text
首页
└─ 运行总览
```

首页隐藏二级侧边栏，只承担运行监控职责：

- 数字孪生展示。
- AMR 实时位置和状态。
- CNC、缓冲区等资源状态。
- 当前任务路径与执行阶段。
- 交通资源占用和等待情况。
- 当前严重告警和业务异常。

首页不提供创建任务、编辑资源或修改策略等配置入口。

## 5. 派单管理

### 5.1 确认后的精简划分

```text
派单管理
├─ 任务中心
│  └─ 任务单详情
├─ 任务配置
│  ├─ 新建任务配置
│  ├─ 编辑任务配置
│  └─ 配置详情
├─ 调度策略
│  └─ 策略详情
└─ 任务日志
   └─ 任务日志详情
```

模块职责：

- 任务中心：从设备请求进入平台开始创建统一任务单，用状态表达校验、配置匹配、调度、执行和最终关闭阶段。
- 任务配置：定义设备请求如何转换为标准物流任务。
- 调度策略：定义任务应如何选择执行 AMR。
- 任务日志：统一记录请求接收、配置匹配、调度决策、行为树节点、交通资源、设备握手以及任务完成或失败事件。

### 5.2 请求与任务的统一模型

设备或 API 请求到达平台后立即创建一张任务单，不再设置独立的请求管理菜单。任务单通过以下状态表达完整生命周期：

```text
待校验 → 待匹配配置 → 待调度 → 已分配 → 执行中 → 已完成
   └→ 参数异常      └→ 匹配失败   └→ 调度失败   └→ 执行失败
```

原始请求编号、请求参数、幂等信息和匹配过程保留在任务日志中，以便解释请求未能进入执行阶段的原因。请求概念在业务数据中保留，但不作为独立前端模块。

### 5.3 任务中心

任务中心使用状态页签统一展示全部任务单：

```text
全部 / 待处理 / 待调度 / 执行中 / 等待中 / 异常 / 已完成
```

任务单详情仅承担现场实时监控，避免把实时状态与历史审计信息混在同一页面。页面由以下区域组成：

- 运行摘要：执行 AMR、当前位置、目标站点、当前行为树节点与总进度。
- 行为树执行工作流：完整展示本任务全部节点，明确已完成、当前运行和等待执行状态。
- 实时信号：AMR 连接、载货传感器、交通资源与目标设备握手状态。

原始请求、匹配配置、调度策略、执行事件、告警、重试、设备握手和最终结果统一进入任务日志。

### 5.4 任务日志

任务日志保留独立二级页面，用于跨任务查询完整业务链事件。它不是单纯的操作日志，而是任务的可追溯事件流，至少包含设备请求、任务配置匹配、候选车辆筛选与调度决策、行为树节点开始与结束、交通资源申请与释放、设备握手、告警、重试以及完成或失败关闭。

单个任务可能产生多次调度决策、资源申请或节点重试，因此日志以任务单号、Trace ID 和时间顺序串联。任务日志详情展示单条事件的输入、输出、关联对象、配置版本和相邻事件；任务单详情只提供前往完整任务日志的入口。

## 6. 行为树管理

```text
行为树管理
├─ 行为树监控
└─ 行为树列表
   └─ 行为树编辑器
```

- 行为树监控：以 AMR 为主体，通过运行卡片墙集中查看来源 CNC、任务、行为树版本、当前节点、进度、等待和异常状态；选择卡片后在同页展示完整节点工作流与当前上下文，不设置独立执行实例详情页。
- 行为树列表：管理行为树模板、版本、适用任务类型、发布状态和任务配置引用关系。
- 行为树编辑器：作为列表后的三级页面，配置控制、条件、动作、设备握手、导航、交通和异常节点。

行为树模板可以编辑，历史执行实例只能查看配置快照，不能反向修改模板。

## 7. 交通监控

```text
交通监控
├─ 实时交通
└─ 交通记录
   └─ 交通记录详情
```

- 实时交通：以 AMR 为入口，在同一地图对比任务最优路径、AMR 当前选择路径与实际行驶轨迹，同时展示重规划点、交通等待和路径偏差。
- 交通记录：以一次任务路径执行为记录粒度，展示规划/实际距离、偏差、重规划次数、交通等待和到达结果，不把高频坐标点直接铺成列表。
- 交通记录详情：回放路径、重规划和交通资源事件，并关联任务单。

交通监控只负责观察和追溯车辆路径及通行状态，不负责选择 AMR、定义物流任务或编辑地图资源。临时封锁属于运行态干预，永久规则必须在地图编辑器的草稿版本中修改。

## 8. 地图管理

```text
地图管理
├─ 地图管理（二级）
│  ├─ 地图卡片库
│  ├─ 创建地图向导
│  ├─ 地图资料编辑
│  └─ 地图编辑器（三级）
│     ├─ 扫描底图图层
│     ├─ 逻辑地图图层
│     ├─ 关联资源定位
│     ├─ 地图校验
│     └─ 发布与 AMR 下发
└─ 地图日志（二级）
   ├─ 创建与资料修改
   ├─ 逻辑地图编辑
   └─ 发布与下发记录
```

- 地图管理：以地图缩略图为主体的卡片库管理地图档案、扫描底图、适用 AMR、现场设备、运行版本、编辑草稿和生命周期。页面不展示运行指标、配置完成度或独立资源关联入口。
- 创建与编辑地图资料时统一维护资源范围。新建流程为“基础信息 → 扫描底图 → 选择适用 AMR 与现场设备 → 创建逻辑地图草稿”。
- 点击地图卡片进入该地图的三级地图编辑器。编辑器使用独立全屏布局，不套用普通后台页面标题和二级侧边导航，也不在编辑器内部切换其他地图。扫描底图作为只读空间参照；用户在逻辑地图中编辑导航道路、站点、设备位置、交通资源和安全区域。
- 编辑器以中央画布为主体，左侧对象树和右侧属性面板可折叠，顶部只保留地图/版本、撤销/重做、校验、保存和发布，底部使用薄型坐标与校验状态栏。
- AMR 与 CNC、缓冲区、充电桩、自动门和电梯等现场设备在地图管理中关联，在编辑器中定位。路口、窄通道、互斥区、会车区和限速区等交通资源直接在编辑器中创建，不设独立资源页面。
- 交通资源必须基于现有道路或绘制区域创建。左侧对象树负责搜索、定位和显隐，画布工具负责创建，右侧属性面板维护关联道路、容量、方向、等待点、超时、释放条件、AMR 组和关联设备。
- 交通资源跟随地图草稿一起校验、发布和下发；不能脱离地图版本单独生效。
- 当前运行版本只读；修改时必须基于运行版本创建草稿，通过校验后发布并下发到指定 AMR。运行中的 AMR 不在行驶中切换地图。
- AMR 内部的地图相似度检测不属于平台地图 UI 范围。
- 地图日志作为独立二级目录，记录创建、资料修改、逻辑编辑、校验、发布、下发和归档事件，并保留地图、版本、操作人、变更内容与结果。

地图负责静态空间定义；实时路径、占用、等待和偏差由交通监控展示。

## 9. 资源管理

```text
资源管理
├─ AMR 管理
│  └─ AMR 详情
├─ 设备管理
│  └─ 设备详情
├─ 站点管理
│  └─ 站点详情
└─ 资源配置
   ├─ AMR 型号
   ├─ AMR 能力标签
   ├─ 设备类型
   └─ 站点类型
```

- AMR 管理：实例维护AMR编号、名称、所属型号、当前地图、初始点位和启用状态，并展示运行状态、电量、位置和任务等实时信息。
- 设备管理：管理 CNC、输送线等具备通信和业务状态的设备。
- 站点管理：管理上料站、缓冲区、充电站、回收站和停靠点。
- 资源配置：AMR型号维护底盘类型、最大线速度、最大角速度、加速度、到达阈值和前视距离；实例继承型号参数，不提供重复覆盖入口。

黄金业务链中的 CNC-01、AMR-01 和 BUF-01 均从资源管理进入详情页。

## 10. 调试平台

```text
调试平台
├─ AMR 调试
├─ API 调试
└─ 调试记录
```

- AMR 调试：选择指定 AMR，通过发布后的逻辑地图选择业务点位或坐标，预览路径并执行受限调试任务。
- API 调试：配置接口请求并查看响应、耗时、错误和 Trace ID，可保存可复用测试用例。
- 调试记录：统一记录 AMR 导航、动作测试、连接诊断和 API 调试操作。
- AMR 调试必须经过空闲状态、生产任务、地图版本、安全装置和控制权检查，并继续使用行为树与交通资源规则。

调试平台只服务研发联调，不作为正常生产任务入口。

## 11. 平台管理

```text
平台管理
├─ 专案管理
├─ 用户管理
├─ 角色权限
├─ 配置管理
├─ 数据字典
├─ 告警规则
└─ 日志管理
   ├─ 操作日志
   ├─ 系统日志
   └─ 接口日志
```

- 专案管理：维护专案、工厂、楼层、地图及资源关系。
- 用户与权限：控制页面访问、配置修改和高风险操作权限。
- 配置管理：维护平台级刷新、保留时间、编号和环境配置。
- 数据字典：统一任务、请求、AMR、设备、事件和告警状态。
- 告警规则：配置告警触发条件、等级和自动处置行为。
- 日志管理：用于技术审计和问题排查，不替代业务事件闭环。

## 12. 统一详情页原则

详情页建议采用统一结构：

```text
对象身份与当前状态
关联业务对象导航
实时监控
业务详情
配置快照或基础配置
事件时间线
```

运行实例与配置模板必须区分：

- 任务详情展示任务配置快照。
- 任务日志中的调度决策事件展示策略参数快照。
- 行为树实例展示行为树版本快照。
- 交通记录展示当时生效的管制规则。
- 修改模板不能改变历史实例和历史记录。

## 13. 建议优化顺序

1. 资源管理：定义 CNC-01、AMR-01 和 BUF-01。
2. 派单管理：确定请求、任务、调度和事件的数据关系。
3. 行为树管理：补齐行为树执行实例。
4. 交通监控：补齐规划路径、实际轨迹、偏差和交通记录。
5. 地图管理：完善扫描底图、逻辑地图、资源空间绑定、版本发布和 AMR 下发。
6. 首页：聚合已经明确的实时业务数据。
7. 调试平台：补齐 AMR 实车调试与黄金业务链接口验证入口。
8. 平台管理：统一专案、权限、字典、告警和日志。

## 14. 协同开发基线

### 14.1 文档职责

| 文档 | 用途 | 主要维护者 |
| --- | --- | --- |
| `系统架构.md` | 定义业务边界、模块职责、页面层级和对象关系 | 产品、架构、前端负责人 |
| `前端UI需求文档.md` | 定义页面内容、字段、状态、交互和验收标准 | 产品、UI、前端开发 |
| `AMR系统设计文档.md` | 保留产品背景、早期方案和扩展能力说明 | 产品经理 |
| `README.md` | 记录原型运行方式、目录和交付入口 | 前端开发 |

发生冲突时，以当前评审通过的 `系统架构.md` 和 `前端UI需求文档.md` 为准。涉及业务边界先更新架构文档，涉及页面展示或交互先更新需求文档，再修改原型。

### 14.2 当前系统边界

当前阶段是研发主导的前端UI原型，目标是验证业务闭环和页面结构，不承担以下内容：

- AMR底层通信协议、品牌适配器和设备驱动。
- SLAM、定位、路径规划、避障和地图匹配算法。
- 调度策略内部算法实现与性能承诺。
- 后端服务拆分、数据库表结构和消息中间件设计。
- 真实生产权限、安全认证、数据持久化和审计合规实现。

原型中的状态、事件、耗时、地图、AMR和设备信息均为演示数据，但命名和关联关系应符合真实业务。

### 14.3 核心对象及主键

| 对象 | 示例主键 | 上游 | 主要下游 |
| --- | --- | --- | --- |
| 专案 | PRJ-001 | 平台配置 | 楼层、地图、资源 |
| 地图 | MAP-A | 专案、楼层 | 点位、道路、交通资源 |
| 设备 | CNC-01 | 专案、地图点位 | 请求、任务 |
| AMR | AMR-01 | 专案、地图 | 调度、行为树实例、交通资源 |
| 设备请求 | REQ-260804-001 | CNC-01 | 任务单 |
| 任务单 | TSK-260804-001 | 设备请求、任务配置 | 调度决策、行为树实例、任务日志 |
| 调度决策 | DSP-260804-001 | 任务单、调度策略 | AMR分配结果、任务日志 |
| 行为树定义 | BT-001 / V1.8 | 行为树列表 | 任务配置、行为树实例 |
| 行为树实例 | BTI-260804-001 | 任务单、AMR | 节点状态、任务日志 |
| 交通资源 | ZONE-B1 | 地图 | 占用记录、等待与冲突 |
| 任务日志 | LOG-001 | 全业务链事件 | 查询、审计、问题定位 |

所有详情页和日志必须使用以上主键建立可追溯链接。`Trace ID` 用于串联一次请求到任务关闭的全部事件，不能替代业务主键。

### 14.4 黄金业务链的模块归属

```text
CNC-01加工完成
  → 设备请求 REQ-260804-001
  → 任务中心创建 TSK-260804-001
  → 任务配置 CFG-002 匹配
  → 调度策略生成 DSP-260804-001 并分配 AMR-01
  → 实例化行为树 BTI-260804-001
  → 执行过程中申请/释放交通资源
  → BUF-01确认接货
  → 任务成功关闭或进入失败处置
  → 全过程写入任务日志
```

模块只展示或维护自己负责的对象：派单管理不编辑地图；行为树不决定选哪台AMR；交通监控不创建业务任务；资源管理不承担任务执行；首页只聚合，不生成新的业务事实。

### 14.5 页面层级规则

- 一级页面通过顶部导航切换，顺序为：首页、派单管理、行为树管理、交通监控、地图管理、资源管理、调试平台、平台设置。
- 二级页面通过左侧导航切换；首页隐藏二级导航。
- 三级页面必须从列表、卡片或记录进入，并提供明确返回按钮，不进入左侧导航。
- 配置模板可编辑；运行实例和历史快照只读。
- 当前全局楼层和地图由顶部统一选择，各模块不得重复提供相同筛选。

### 14.6 状态与事件原则

- 状态描述当前事实，例如“待调度、执行中、等待、异常、已完成”。
- 事件描述已经发生的变化，例如“请求已接收、AMR已分配、节点执行失败”。
- 同一状态在不同页面必须使用相同名称、颜色和含义。
- 任务关闭必须有成功或失败结果、结束时间和最后一个业务事件。
- 历史记录保存创建时的配置、策略、行为树和管制规则版本，不随模板修改。

## 15. 前后端协作约定

### 15.1 页面数据状态

每个数据页面至少考虑以下前端状态：加载中、正常、有部分异常、空数据、请求失败和无权限。实时监控页面还需显示最后更新时间、连接状态和数据是否过期。

### 15.2 时间与刷新

- 时间统一展示为本地时间，完整日志使用 `YYYY-MM-DD HH:mm:ss.SSS`。
- 运行时长使用 `HH:mm:ss`，短时节点允许使用秒。
- 实时页面显示“实时更新”或最后更新时间；原型只模拟视觉，不承诺真实刷新周期。

### 15.3 危险操作

暂停任务、终止任务、释放交通资源、发布地图、发布行为树和停用资源属于高风险操作。正式实现时必须包含权限判断、二次确认、影响说明和操作日志；原型可以只展示入口和确认反馈。

### 15.4 变更流程

1. 产品在需求文档中更新页面目的、字段或交互。
2. 涉及模块边界或对象关系时同步更新架构文档。
3. 前端修改共享组件、页面和演示数据。
4. 自测语法、页面链接、主要屏幕尺寸和三级页面返回路径。
5. 评审通过后提交Git，并在提交说明中标明受影响模块。
