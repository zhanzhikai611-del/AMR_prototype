# FXXXXXN AMR CONTROL 前端UI需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档用途 | UI原型、前端实现与产品验收基线 |
| 当前阶段 | MVP前端原型 |
| 业务范围 | 厂内CNC加工线AMR物流 |
| 首期设备 | 单一品牌AMR，平台直接调度单车 |
| 技术形态 | 独立HTML、共享CSS和Vanilla JavaScript，无第三方运行依赖 |
| 业务基线 | CNC加工完成，请求AMR将成品搬运到缓冲区 |

本文档只定义页面功能、信息展示、交互和状态，不定义通信协议、后端接口实现、调度算法、SLAM或路径规划算法。

## 2. 用户与使用场景

| 用户 | 核心任务 | 常用页面 |
| --- | --- | --- |
| 现场人员 | 查看AMR、任务、交通和异常，快速定位阻塞 | 首页、任务中心、行为树监控、交通态势 |
| 调度/实施人员 | 配置任务、策略、行为树、地图和资源 | 派单管理、行为树列表、地图管理、资源管理 |
| 研发测试人员 | 查看技术日志并发起接口测试 | 任务日志、API测试、系统日志 |
| 管理员 | 管理用户、权限、配置和字典 | 平台设置 |

## 3. 全局框架

### 3.1 一级导航

```text
首页 → 派单管理 → 行为树管理 → 交通管制 → 地图管理
     → 资源管理 → API测试 → 平台设置
```

### 3.2 全局运行范围

顶部栏提供唯一的“楼层 / 地图”选择入口。切换后影响首页、任务、行为树、交通、地图和资源页面。各业务页面不得增加功能重复的楼层或地图筛选。

### 3.3 页面层级

- 一级模块：顶部导航。
- 二级页面：左侧导航。
- 三级页面：由列表、卡片或记录进入，使用返回按钮回到上一级。
- 首页：隐藏左侧导航，监控内容铺满主区域。

### 3.4 视觉与布局

- 采用浅色、简洁、工业SaaS风格。
- 内容页使用浅灰网格背景、白色卡片和蓝色主操作。
- 首页使用静态数字孪生车间全屏背景，两侧看板以半透明磨砂层悬浮。
- 红色只用于严重、失败或危险操作；黄色用于等待和警告；绿色用于正常和成功；蓝色用于运行中和选中状态。
- 页面标题下不展示重复解释文字。

## 4. 黄金业务链验收场景

### 4.1 前置数据

- 当前范围：`2F / MAP-A`。
- 请求设备：`CNC-01`。
- 执行车辆：`AMR-01`。
- 目标设备：`BUF-01`。
- 任务配置：`CFG-002 成品转运 V1.3`。
- 调度策略：`STR-001 最近距离优先 V1.6`。
- 行为树：`BT-001 成品下料流程 V1.8`。

### 4.2 业务步骤

1. CNC-01完成加工并发出请求。
2. 平台创建任务单并匹配任务配置。
3. 调度策略选择AMR-01。
4. 平台创建行为树执行实例。
5. AMR前往CNC-01并完成取货。
6. 执行过程中持续申请、占用和释放交通资源。
7. AMR到达BUF-01并完成放货。
8. BUF-01确认接货，任务成功关闭；任一步失败则进入异常关闭或人工处理。
9. 全过程可在任务日志中按任务单和Trace ID追溯。

### 4.3 前端验收结果

- 首页能够看到AMR、任务和异常概览。
- 任务中心能够定位任务单并进入实时详情。
- 任务详情能够看到当前AMR、节点、位置、目标和完整行为树进度。
- 行为树监控能够从AMR卡片定位同一执行实例。
- 交通管制能够看到相关资源的占用或等待状态。
- 资源管理能够进入CNC-01、BUF-01和AMR-01详情。
- 任务日志能够还原请求、匹配、调度、执行、交通、握手和关闭事件。

## 5. 页面需求

### 5.1 首页

### 页面目的

在单屏内展示当前全局范围的运行态势，不承担配置或快捷创建任务。

### 页面内容

- 静态CNC车间数字孪生背景，覆盖全部主内容区域。
- 左侧看板：AMR运行概览、电量分布、AMR状态。
- 右侧看板：当前任务、交通资源、运行异常。
- 看板保持半透明，不能遮蔽全部车间背景。

### 交互

- AMR条目进入AMR详情。
- 任务条目进入任务详情。
- 异常条目进入相关日志或对象详情。
- 页面不显示重复的楼层地图信息卡片。

### 5.2 派单管理

### 二级页面

```text
任务中心
任务配置
调度策略
任务日志
```

### 任务中心

- 统一展示设备请求转换形成的任务单，不另设请求管理页面。
- 状态至少包含：待处理、待调度、执行中、等待、异常、已完成。
- 主要字段：任务单号、请求编号、来源设备、路线、当前阶段、执行AMR、状态、持续时间。
- 点击任务进入三级任务详情。
- 不提供手工补单入口。

### 任务详情

只展示实时运行监控，不混入历史配置表单：

- 任务身份、运行状态和持续时间。
- 执行AMR、当前位置、目标站点、当前节点、总体进度。
- 完整行为树节点工作流，区分已完成、当前、等待、失败和跳过。
- AMR连接、载货传感器、交通资源和目标设备握手等实时信号。
- 提供地图定位、暂停任务和查看任务日志入口。

### 任务配置

- 定义设备请求如何转换成标准任务。
- 字段包括配置编号、名称、请求来源类型、起点、目标点、能力要求、行为树、策略、优先级和超时规则。
- 新建和编辑为三级页面。
- 已被运行实例引用的历史版本不可直接覆盖。

### 调度策略

- 管理研发提供的策略类型和可配置参数。
- 列表显示名称、版本、策略类型、引用数量、状态和更新时间。
- 详情为三级页面，允许配置参数并展示模拟候选结果。

### 任务日志

- 统一记录设备请求、配置匹配、调度决策、行为树节点、导航、交通资源、设备握手、告警、重试和任务关闭。
- 字段包括时间、日志编号、任务单、日志类型、关联对象、行为节点/事件、说明和结果。
- 操作列统一使用“查看”。
- 日志详情展示事件内容、上下文快照和相邻事件。

### 5.3 行为树管理

### 二级页面

```text
行为树监控
行为树列表
  └─ 行为树编辑器（三级）
```

### 行为树监控

- 仅采用AMR视角，不建立设备视角。
- 上方展示执行中、等待、异常和今日完成数量。
- 中部使用AMR运行卡片墙，显示来源CNC、目标设备、任务、树版本、当前节点、进度和耗时。
- 点击卡片后在同页切换完整节点工作流及当前节点上下文，不跳转三级实例页。
- 当前节点上下文包含来源、目标、节点耗时、交通资源和重试次数。
- 不重复提供楼层地图筛选。

### 行为树列表与编辑器

- 列表展示编号、名称、流程摘要、版本、发布状态、引用范围和更新时间。
- “新建”或“编辑”进入三级编辑器。
- 已发布版本只读，修改时创建新草稿；运行实例固定使用创建时版本。
- 编辑器由节点库、画布和节点属性三部分组成。

### 5.4 交通管制

### 交通态势

- 展示AMR、道路、交通区域、占用、等待队列、封锁和冲突。
- 关注运行秩序，不负责派单或任务创建。

### 管制资源

- 管理路口、窄通道、会车区、缓冲区入口和封锁区。
- 展示资源类型、地图位置、容量、占用者、等待者和当前状态。

### 管制记录

- 记录资源申请、授权、占用、释放、超时、冲突和人工干预。
- 支持按AMR、任务、资源和结果查询。

### 5.5 地图管理

### 地图编辑器

- 作为模块默认页面，直接编辑顶部全局范围选中的地图。
- 使用独立静态点阵图，不与交通态势共用业务地图表现。
- 支持点位、道路、区域、设备映射、测距、图层、校验、草稿和发布。
- 支持导入AMR扫描地图和显示地图版本。

### 地图列表

- 负责创建空地图记录以及管理楼层、来源、版本和发布状态。
- 创建成功后通过顶部全局范围切换到新地图，再进入编辑器维护。
- 后续可扩展扫描记录、相似度对比和偏差告警。

### 5.6 资源管理

### 二级页面

```text
AMR列表
设备列表
型号配置
设备类型
```

- AMR和设备详情均为列表后的三级页面，不进入左侧导航。
- AMR详情包括连接、电量、位置、任务、运行状态和地图定位。
- 设备详情包括设备状态、连接、地图点位、请求信号和关联任务。
- 首期保留单一品牌结构，字段设计应允许未来增加品牌、型号和能力标签。

### 5.7 API测试

- 接口目录：按模块浏览可测试接口。
- 测试工作台：编辑方法、路径、Header、参数和Body，展示响应和耗时。
- 请求历史：查看并复用历史请求。
- 仅用于研发联调，不代替正式接口文档和自动化测试平台。

### 5.8 平台设置

- 用户管理：用户、状态和角色。
- 角色权限：模块访问、配置修改和高风险操作权限。
- 配置管理：前端可见的平台级参数。
- 数据字典：统一状态、类型、等级和显示文案。
- 操作日志：记录用户操作。
- 系统日志：用于技术问题定位。

## 6. 统一状态规范

| 类型 | 状态 | 建议颜色 |
| --- | --- | --- |
| 任务 | 待处理、待调度、等待 | 黄色或灰色 |
| 任务 | 执行中 | 蓝色 |
| 任务 | 已完成 | 绿色 |
| 任务 | 异常、失败 | 红色 |
| AMR | 空闲、在线 | 绿色 |
| AMR | 执行中 | 蓝色 |
| AMR | 充电、等待 | 青色或黄色 |
| AMR | 故障、离线 | 红色或灰色 |
| 行为节点 | 已完成 | 绿色 |
| 行为节点 | 当前执行 | 蓝色并突出 |
| 行为节点 | 等待执行、跳过 | 灰色 |
| 行为节点 | 失败 | 红色 |
| 交通资源 | 空闲 | 绿色或灰色 |
| 交通资源 | 占用 | 蓝色 |
| 交通资源 | 等待、冲突 | 黄色或红色 |

## 7. 通用交互要求

- 可点击的表格行、卡片和文本链接需要统一悬停反馈。
- 三级页面必须提供“返回列表”或“返回任务中心”。
- 搜索和筛选只保留有独立业务意义的条件，不重复全局范围。
- 空数据需要说明原因及下一步入口；加载失败提供重试。
- 危险操作使用红色描边或按钮，并在正式实现中二次确认。
- 表格操作名称使用明确动词，例如“查看、编辑、测试、复用”，避免单独省略号菜单。

## 8. 响应式与兼容要求

- 主要评审分辨率：1920×1080和1366×768。
- 首页优先单屏展示，不产生纵向滚动；窄屏允许压缩侧看板宽度。
- 数据表格列不足时允许水平滚动，不应把主要操作裁切成省略号。
- 当前原型支持现代Chrome和Edge。
- 使用本地相对路径，保证直接打开和GitHub Pages部署均可用。

## 9. 页面验收清单

- 一级导航顺序与架构文档一致。
- 二级页面及三级返回路径正确。
- 所有页面标题下无重复解释文案。
- 全局楼层地图筛选未在模块内重复出现。
- 首页背景、两侧看板和透明度符合评审版本。
- 黄金业务链中的编号和对象能够互相对应。
- 任务详情只显示实时监控；历史信息进入任务日志。
- 行为树监控采用AMR卡片墙并支持选中切换。
- AMR和设备详情只能从列表进入。
- 页面链接无失效目标，JavaScript无语法错误。

## 10. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-04 | V0.1 | 建立当前MVP前端UI协同开发基线 |
